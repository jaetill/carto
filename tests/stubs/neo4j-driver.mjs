// Stub for neo4j-driver — real package lives in lambda/node_modules.
// This file exists only so Vite can resolve the import path during test analysis;
// tests mock this module via vi.mock() before any code runs.
export default { driver: () => {}, auth: { basic: () => ({}) }, Integer: {} };
